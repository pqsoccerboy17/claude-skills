# =============================================================================
# Ecosystem Configuration
# =============================================================================
# Central configuration for all automation tools
#
# SETUP:
#   1. Copy this file: cp ~/scripts/ecosystem.env.example ~/scripts/ecosystem.env
#   2. Fill in your credentials
#   3. Add to ~/.zshrc: source ~/scripts/ecosystem.env
#
# SECURITY:
#   - Never commit ecosystem.env to git (only the .example file)
#   - Use keychain/keyring for highly sensitive credentials where possible
# =============================================================================

# -----------------------------------------------------------------------------
# Notion API
# -----------------------------------------------------------------------------
# Required by: treehouse-context-sync, drive-index-sync, notion-rules
# Get token from: https://www.notion.so/my-integrations

NOTION_TOKEN=

# Database IDs (found in Notion page URLs)
NOTION_DOCUMENT_INDEX_DB_ID=
NOTION_PROPERTIES_DB_ID=
NOTION_DOCS_PARENT_PAGE_ID=

# Optional: limit sync scope
CONTEXT_SYNC_MAX_ITEMS=200

# -----------------------------------------------------------------------------
# Google Drive API
# -----------------------------------------------------------------------------
# Required by: drive-index-sync
# Setup: Create service account at console.cloud.google.com

# Paste full JSON or base64-encoded JSON
GDRIVE_SERVICE_ACCOUNT_JSON=
# OR
GDRIVE_SERVICE_ACCOUNT_JSON_B64=

# Root folder ID or URL to scan
DRIVE_ROOT_FOLDER=

# Optional: workspace filter
DRIVE_SYNC_WORKSPACE=

# Optional: dry run mode (set to "1" to preview without writing)
DRIVE_SYNC_DRY_RUN=0

# -----------------------------------------------------------------------------
# Pushover Notifications
# -----------------------------------------------------------------------------
# Required by: notify.py (used by tax-pdf-organizer, etc.)
# Setup: https://pushover.net

PUSHOVER_USER_KEY=
PUSHOVER_APP_TOKEN=

# Optional: disable all notifications
NOTIFY_ENABLED=true

# -----------------------------------------------------------------------------
# Monarch Money
# -----------------------------------------------------------------------------
# Note: Monarch uses session-based auth via login_setup.py
# Session file: ~/Library/Application Support/monarch-mcp-server/mm_session.pickle
# Run login_setup.py to authenticate

# Optional: MCP server port
MONARCH_MCP_PORT=8765

# -----------------------------------------------------------------------------
# Paths (usually don't need to change)
# -----------------------------------------------------------------------------

# Where tax organizer stores files (Google Drive synced folder)
# TAX_BASE_FOLDER is hardcoded in organize_statements.py
# Modify directly in script if needed

# Scripts location
SCRIPTS_DIR="${HOME}/scripts"

# =============================================================================
# Usage Examples
# =============================================================================
#
# Check current configuration:
#   python3 ~/scripts/ecosystem_status.py
#
# Run drive index sync (dry run):
#   DRIVE_SYNC_DRY_RUN=1 python3 ~/Documents/treehouse-context-sync/scripts/drive_index_sync/sync_drive_index.py
#
# Send test notification:
#   python3 ~/scripts/notify.py "Test" "Hello World"
#
# =============================================================================
